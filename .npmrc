# Always install devDependencies — Render sets NODE_ENV=production which
# would otherwise cause pnpm to skip them, breaking Next.js builds that
# need tailwindcss, postcss, autoprefixer, prisma, etc. at compile time.
production=false
